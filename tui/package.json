{
  "name": "@magentra/tui",
  "version": "0.17.1",
  "private": true,
  "type": "module",
  "description": "Terminal frontend for the MAGENTRA engine — a pure NDJSON protocol client (ink). Ships inside the desktop artifacts as resources/engine/tui.mjs and runs through Electron's own Node.",
  "scripts": {
    "start": "tsx src/cli.tsx",
    "dev": "tsx watch src/cli.tsx"
  },
  "dependencies": {
    "ink": "^5.1.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
